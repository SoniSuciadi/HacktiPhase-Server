{
  "name": "forum",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "devStart": "nodemon app.js",
    "test": "cross-env NODE_ENV=test jest --testTimeout=10000 --runInBand --forceExit --verbose --coverage",
    "pretest": "cross-env NODE_ENV=test",
    "db:create:test": "cross-env NODE_ENV=test npx sequelize-cli db:create",
    "db:drop:test": "cross-env NODE_ENV=test npx sequelize-cli db:drop",
    "db:migrate:test": "cross-env NODE_ENV=test npx sequelize-cli db:migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "pg": "^8.8.0",
    "sequelize": "^6.24.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.1.2",
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.5.1",
    "supertest": "^6.3.0"
  }
}
